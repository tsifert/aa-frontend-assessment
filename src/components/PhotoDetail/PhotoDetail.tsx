import { ReactElement } from "react";
import './PhotoDetail.css';

const PhotoDetail = (): ReactElement => {

    return (
        <div className="photoDetailMain">
            <span>Detail</span>
        </div>
    )
};

export default PhotoDetail;